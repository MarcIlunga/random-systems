Prove the theorem `chain_bound` in
`eval/cases/case2-adjacent-sum-reuse/Fixture.lean`.

It says: if consecutive systems along a trace are `eps i`-close in maximal
distinguishing advantage, then the two endpoints are `∑ eps`-close.

Replace the `sorry` with a real proof. The file must compile.
