Blocked by the frozen theorem’s `epsKS` call.

Completed axiom-clean definitions for:

- [`sequenceMACSystem`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:52)
- [`framedNMACSystem`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:79)

Reused `headerI`, `headerO`, `derive`, `encodeItems`, `encodeMSBF`, `mdHash`, `mdIterate`, `Dist.uniform`, `Dist.prodProbDist`, and `PFunPDS.Prob.functionEvaluator`.

The blocker: a concrete `epsKS := Adv (q := q) …` necessarily has an implicit `q`, but the frozen theorem calls:

```lean
epsKS codec iv L digestBytes b κ hκU128 S f
```

Lean cannot infer `q`, producing:

```text
FiniteTranscriptSpace InputSequence C ?q
```

The minimal required theorem change is:

```lean
epsKS (q := q) codec iv L digestBytes b κ hκU128 S f
```

I left the theorem byte-identical and restored `epsKS` to its stub. The target build is green; `sorry` currently remains on `epsKS`, `omegaS`, and the theorem. No additional modeling parameter is required.