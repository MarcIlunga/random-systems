Implemented [SequenceMACIndiff.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:1).

- Added finite active-call domain, its `Fintype`, active `PFunPDS.URF`, extension to `FixedHash`, and `sequenceMACRODist`.
- Added `SM_H` and `SM_RO` using the existing `sequenceMACSystem` converter realization.
- Expressed the actual budgeted abstract-crypto hypothesis with `ProtocolIndifferentiable` and exposed its `.constructs` consequence.
- Recorded the unavailable PFunPDC/metric transport as the named `ProtocolIndifferentiableSequenceMACBridge`; no composition was fabricated.
- Defined `ε_enc = ε_sched + pairCollisionUnionBound` and recorded the R1 parsing/adaptive scheduling work as `SequenceMACEncodingBound`.
- Added the guardrail [sequenceMAC_prf_bound_indiff](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:226):
  ```lean
  Δ(⌈q⌉ SM_H b S DK D_H, ⌈q⌉ PFunPDS.URF)
    ≤ (ε_ind (4 * q) : ℝ) + (ε_enc ε_sched q : ℝ)
  ```
  Its proof is the sole intended `sorry`.

Reused: R1 parsing surface, `sequenceMACStep`/`sequenceMACSystem`, `PFunPDS.URF`, `pairCollisionUnionBound`, and the actual abstract-crypto API. New: the finite active-domain wrapper and the two explicitly named bridge/scheduling obligations.

`lake build SequenceHash` passes: 8332 jobs. No `private`, forbidden advantage/game vocabulary, or edits to the proven R1/R2 declarations.