TASK P4i.fibercard — R4 step 3 CRUX: prove the SequenceFunction analog of `cbc_fiber_card`
(the `hprod` mass-factorization), then apply the GENERIC `condEquiv_of_transcript_mass_reductions`
to obtain and package the R4 equality-on-good. This is the last piece of step 3. Sorry-free.
NO `B_SEQ`/`h_badmass`/final theorem (step 4).

THE TEMPLATE + THE GENERIC ENGINE (both exist — reuse, don't reprove):
- `cbc_fiber_card` (`RandomSystems/CBCMAC.lean:468`) is the template: it proves the fiber-cardinality
  factorization `|{f : state-assignment = a ∧ P f}| · |X|^(#inputs) = |{f : P f}|` (i.e. the fresh
  state-assignments are UNIFORM among bad-event-free f). It does this by REUSING the GENERIC counting
  lemma `Counting.card_filter_shift_univ` plus a CBC "shift" (`cbcShift`) with three properties:
  state-shift (`cbcState_cbcShift`), shift-compose (`cbcShift_cbcShift`), shift-zero (`cbcShift_zero`).
- `condEquiv_of_transcript_mass_reductions` (`RandomSystems/SwitchingLemma.lean`) is GENERIC; its only
  protocol-specific premise is `hprod` (the factorization). Supply `hprod` from the fiber-card below;
  the other premises (`hAf`/`hY`/`hYAf`) are the mechanical mass reductions.

DELIVER (sorry-free, reusing `Counting.card_filter_shift_univ` — NOT a new counting engine):
1. `sequenceFunctionTerminalShift` — the re-randomization of the TERMINAL construction compression
   coordinate (the outer call's fresh table entry), analog of `cbcShift`. Prefix-freeness of the
   framed outer input (proven, `DomainSeparation`) + the `OutputCompressionBacked` fact (step 1) make
   it well-defined; domain separation makes it leave the inner/derive coordinates fixed.
2. Its three group-action properties (state-shift, compose, zero) — analogs of the `cbc*` lemmas;
   domain separation should make these clean (the terminal coordinate is independent of the rest).
3. `sequenceFunction_fiber_card` — the factorization, proved via `Counting.card_filter_shift_univ`
   with `P := ¬ Bad_SEQ`-style freshness (reuse `sequenceFunctionConstructionCall_fresh_of_not_Bad_SEQ`
   for `hPfresh`, and (1)/(2) for `hPshift`).
4. Discharge `hprod` from (3); apply `condEquiv_of_transcript_mass_reductions` to get the
   per-transcript extended-mass equality off `Bad_SEQ` for budget-respecting transcripts; PACKAGE it
   through `adv_le_of_extFixedQueryRep_eq_on_good_filtered_of_filter` ⇒ the named R4 equality-on-good
   lemma. After this, step 4 IS the only remaining R4-spine work.

READ FIRST: `CBCMAC.lean:468` (`cbc_fiber_card`) + the `Counting.card_filter_shift_univ` it calls;
`condEquiv_of_transcript_mass_reductions` in `SwitchingLemma.lean`; `SequenceMACGenericFiber.lean`
(3a freshness) + `SequenceMACGenericFiberMass.lean` (3b reveal/reduction + the honest-prefix part
already done); `DomainSeparation.lean` (prefix-freeness) + `SequenceMACGenericTrace.lean`
(`OutputCompressionBacked`).

ADDITIVE: extend `SequenceMACGenericFiberMass.lean`. Do NOT modify 3a, the facade, steps 1–2, R1–R6.

ACCEPTANCE: whole-repo `lake build` green; axiom-clean {propext, Classical.choice, Quot.sound};
sorry-free / axiom-free / no `private`; additive; reuse `Counting.card_filter_shift_univ` +
`condEquiv_of_transcript_mass_reductions` + 3a/3b + `DomainSeparation` by name (grep to confirm no
reproved counting/condEquiv engine); the R4 equality-on-good lemma is now PROVEN. NO `B_SEQ`/
`h_badmass`/final theorem. Report: the shift + its 3 properties, `sequenceFunction_fiber_card`, the
packaged R4 eq-on-good, which generic lemmas were reused, the axiom line, and confirm step 4 is all
that remains.

*** VALVE: if the shift-property proofs are large, STOP after `sequenceFunctionTerminalShift` + its
3 properties (sorry-free) and report the remaining fiber-card + packaging plan. Never leave a `sorry`. ***
