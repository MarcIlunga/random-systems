Implemented T5.enc in [SequenceMACIndiff.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:430).

Key result:

- `sequenceMAC_encoding_bound` establishes `SequenceMACEncodingBound` using the concrete `sequenceMACScheduleError`.
- The scheduling residual is the exact filtered `Δ` between `SM_RO` and the role-separated RO schedule.
- The remaining birthday term is `pairCollisionUnionBound (HashOutput L) q`.

Reuse map:

- `maxAdvantage_filterQueries_seededHashThenURF_le` for condition C/adaptive reduction.
- `MACPRF.uniform_fixedQuery_collision_le` for the existing birthday leaf.
- `encodeItems_injective` for keyed inner-input unambiguity.
- `sequenceMACScheduledCall_mem` and existing active-domain plumbing.
- `Dist.mass_prod_snd` for independent-seed marginalization.
- `maxAdvantage_triangle` for schedule residual composition.

No condition-C reproof was added. `uniform_restrict` was unnecessary because the role-separated inner component is sampled directly as a URF on `InputSequence`.

Verification:

- `lake build SequenceHash` — green.
- Fresh axiom check: `propext`, `Classical.choice`, `Quot.sound` only.
- No `private`, `advPRF`, `advNPRF`, `Adv`, `sorry`, or `admit`.
- Frozen R1/R2 and R3 guardrail statements remain unchanged.

Residual: reducing `sequenceMACScheduleError` to zero requires the separate derivation-freshness/short-input premise described by A4.