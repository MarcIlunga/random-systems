TASK P4a.facade — R4 FACADE INFRASTRUCTURE (dispatch 1 of the accepted R4 split; the
structural theorem is the separate dispatch 2). Build the reusable ideal-compression
H-technique facade so the R4 tight-generic-PRF theorem can be STATED and its equality-on-good
spine PROVEN next. R4 is the H-technique route — `advPRF`/`HTechnique/*` allowed.

ADDITIVE-SAFE — non-negotiable:
- NEW module(s) only (e.g. `RandomSystems/HTechnique/IdealCompression.lean` for the generic
  facade; a SequenceHash file only if a piece is scheme-specific). Do NOT modify R1/R2/R3/R5/
  DomainSeparation or any existing proof.
- If `EnvRespects` (or another shared H-technique def) must be generalized, do it NON-BREAKING:
  add a new public generalization with the existing one recovered as a special case (or a
  parallel public def) — every existing `EnvRespects` user MUST still build unchanged. Grep
  its call sites first; do NOT rewrite them.
- `All.lean`: ADDITIVE import only (append), never reorder/remove. Prefer leaving the new
  module out of a coverage audit over editing shared files.
- Whole-repo `lake build` MUST stay green; nothing downstream (R2 engine, StrongPRP,
  Derivation) may break.

DELIVER (Codex's own split plan, item 1):
1. `TaggedBudgetRespects p q lambda` — a reusable tagged `Prim`/`Eval` query
   accounting + budget/cost predicate (separate `Prim` count `p`, `Eval` count `q`, per-`Eval`
   trace cost `lambda`; total transcript length fixed to `p + q`). A GENERALIZATION of
   `EnvRespects` (non-breaking, reusable) — NOT a `SequenceMACEnvRespects` local copy.
2. The generic filtered equality-on-good representative endpoint: REUSE
   `adv_le_of_extFixedQueryRep_eq_on_good` (or its ratio sibling at epsilon 0); add only a
   thin tagged-filtered wrapper if the existing endpoint does not directly accept the tagged
   budget. Do NOT reprove the fundamental/coupling theorems.
3. The generic joint ideal-compression resource shape: typed ideal-compression coins, the
   shared-`Prim` real/ideal worlds (shared `Prim` interface present in BOTH), canonical
   `sequenceFunction` evaluation on the `Eval` side (start from canonical `SequenceFunction` —
   never a duplicate NMAC-shaped construction), and the totality + extension/reveal carrier
   lemmas needed to talk about good-transcript equality.
4. Freeze the missing parameters: the codec/IV/digest of the compression model and the exact
   TYPE of `SequenceFunctionTraceBound` (so dispatch 2 can state the theorem against them).

STATEMENT-FIRST NOTE: the endpoint in (2) is a real theorem; the defs support it. Everything
delivered must be sorry-free and axiom-free — no downstream R4 obligation is discharged here.

ACCEPTANCE: whole-repo `lake build` green; every new theorem axiom-clean {propext,
Classical.choice, Quot.sound}; sorry-free / axiom-free / no `private`; ADDITIVE (new files;
verify existing files' mtimes unchanged except an additive `All.lean` import if truly needed);
any `EnvRespects` generalization is non-breaking (grep confirms existing users untouched and
still building); the facade REUSES the extended-representative/hash-then-PRF spine (no reproved
fundamental theorem). Report: the new module(s), the facade defs + the endpoint theorem, the
frozen codec/IV/`SequenceFunctionTraceBound` types (for dispatch 2), the reuse map, axiom line.

*** IF item 3 (the ideal-compression worlds + reveal carriers) is genuinely too large to
finish sorry-free alongside 1/2/4 in one dispatch, STOP and report a finer split rather than
leaving a `sorry`. ***
