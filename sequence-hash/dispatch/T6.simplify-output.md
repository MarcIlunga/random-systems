Stopped under the mandated missing-framework/invalid-statement guardrail. No files were changed.

The requested pure-birthday encoding leg is not derivable for the current `SM_RO`:

- [`SM_RO`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:143) uses one shared active-domain RO table for raw derivation, inner, and outer calls.
- `encodeItems_injective` proves distinct messages have distinct inner inputs. It does not prove raw `K`/`S` inputs are disjoint from framed inner/outer inputs.
- The reusable [`maxAdvantage_filterQueries_seededHashThenURF_le`](/Users/marcilunga/Documents/tob/research/random-systems/RandomSystems/SwitchingLemma.lean:2041) explicitly samples the seed independently of the outer URF. There is no framework lemma transporting the shared-table `SM_RO` to that independent model.
- At `q = 1`, the requested birthday bound is zero. It would therefore assert exact one-query equivalence for arbitrary long keys/customizations, despite possible cross-role repeated RO inputs. No freshness premise excludes those repetitions.

The smallest sound repair is either:

1. Add a cross-role freshness/disjointness hypothesis plus a framework lemma splitting a uniform table over those disjoint domains; then the encoding leg is pure birthday.
2. Add an explicit probability bound for the raw/framed overlap event.
3. Redefine `SM_RO` as the role-separated independent model, which would no longer be the retained active-domain composition.

Baseline verification: `lake build SequenceHash` remains green, 8,332 jobs.