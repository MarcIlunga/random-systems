Partial completion only.

- [`gazi_outer_prf_hop`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:1018) is fully proved.
  - Fixes the inner key and represents the outer call as a one-query simple converter.
  - Reuses `maxAdvantage_filterQueries_applyDDC_le` with budget `q * 1 = q`.
  - Disintegrates the independent key product and averages the pointwise DPI bounds using bilinearity of `verdictProb`.
  - Also reuses `PFunPDS.applyDDC_simple_ofFunDist`, `advantage_le_maxAdvantage`, and `maxAdvantage_le_of_forall_advantage_le`.
  - Axiom-clean: `propext`, `Classical.choice`, `Quot.sound`; no `sorryAx`.

- [`gazi_outer_random_collision_bound`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:1245) remains the single collision-half `sorry`.
  - The missing step is Gaži §3.1 condition \(C\) and Eq. (2):
    \[
    \nu^A(\mathrm{Casc}_{K_1}^f.r,C_q)
      = \nu^{A_{\mathrm{na}}}(\mathrm{Casc}_{K_1}^f.r,C_q).
    \]
  - It must then perform the one-block prefix-free extension, apply `hcascade`, and add the URF collision probability through `pairCollisionUnionBound`/`pairCollisionUnionBound_le_birthday`.
  - The public birthday primitives exist, but CBC’s condition-equivalence and first-collision machinery are private and CBC-specific; there is no reusable generic seed-indexed hash-then-URF condition-\(C\) theorem yet.

Verification:

- `lake build SequenceHash.RandomSystems.SequenceMACPRF` passes.
- The implementation remains pure `PFunPDS`/`Δ`/`⌈q⌉`; no `advPRF`, `advNPRF`, or `Adv`.
- `nmac_prf_bound` is not yet sorry-free: its remaining `sorryAx` comes solely through `gazi_outer_random_collision_bound`.
- The frozen `sequenceMAC_prf_bound` body remains untouched and still has its pre-existing `sorry`.