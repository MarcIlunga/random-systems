Implemented both Proposition 1 hybrids in [SequenceMACPRF.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:310).

- `gazi_lemma6_row_hybrid` — Gaži Eq. (10):
  - Builds the \(q\) row-index hybrids.
  - Isolates one changed row with product laws and applies `CompNASecure`.
  - Uses fixed-query transcript laws, `statDist_prod_left`, DPI, and the generalized adjacent-law telescope.
  - Produces the factor \(q·ε_{na}\).

- `gazi_lemma5_depth_hybrid` — Gaži Eq. (9):
  - Builds the length-position hybrids using one uniform chaining value per distinct prefix.
  - Uses prefix-freeness to separate completed queries from longer prefix classes.
  - Reduces each adjacent hop to `multiCompReal` versus `multiCompIdeal`.
  - Uses `MDHash.mdIterate_append` for the cascade split.
  - Handles both endpoints and the empty-query case, producing factor \(d\).

Shared generalized helpers cover uniform restriction, independent product mapping, and law-level telescoping. `AdvantageSeq` operates on system-level `Δ`, whereas these frozen statements are fixed-query `statDist`; the corresponding law-level telescope therefore uses the existing `statDist_triangle` without introducing H-technique objects.

Validation:

- `lake build SequenceHash.RandomSystems.SequenceMACPRF` passes.
- Both lemmas’ axiom audits contain only `propext`, `Classical.choice`, and `Quot.sound`; no `sorryAx`.
- No `advPRF`, `advNPRF`, or `Adv` usage.
- Remaining untouched sorries:
  - `gazi_outer_prf_hop`: §3.1 outer PRF hop toward Eq. (1).
  - `gazi_outer_random_collision_bound`: §3.1 Eq. (2), collision reduction, and birthday term.
  - `sequenceMAC_prf_bound`: final SequenceMAC triangle, not part of Gaži Proposition 1.