Implemented in [SequenceMACIndiffMD.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiffMD.lean:1).

Key additions:

- Canonical `sequenceFunction` and `sequenceFunctionStep`, with existing SequenceMAC proved definitionally equal to the `F = 1` specialization.
- `mdHashDist` as the pushforward of `Df` through `mdHash codec · iv`.
- `drstError = 13·Σ²/2^(8L)`.
- `sequenceFunctionCompressionCost`, taking the finite maximum compression-block cost over at most `Q` canonical hash calls, plus `p` primitive calls.
- `DerivePrefixHit_SEQ`, `DeriveCost_SEQ`, and `DeriveSafeS_SEQ`.
- `sequenceMAC_md_h_indiff`, deriving the frozen R3 hypothesis from explicit `h_drst` and `hepsilon`.
- `sequenceMAC_md_prf_bound_indiff`, composing it with `sequenceMAC_prf_bound_indiff` to obtain:
  `ε_ind (4*q) + pairCollisionUnionBound (HashOutput L) q`.

Reused by name:

- `sequenceMACDeriveInput_ne_innerInput_of_not_prefix`
- `sequenceMACDeriveInput_ne_outerInput_of_not_prefix`
- `SM_H`, `SM_RO_separated`
- `sequenceMAC_prf_bound_indiff`
- `pairCollisionUnionBound`

Verification:

- `lake build SequenceHash`: green, 8,319 jobs.
- `lake build`: green, 8,440 jobs.
- New file contains no `private`, `sorry`, `admit`, or `axiom`.
- Protected R1/R2/R3/DomainSeparation-related source mtimes remained unchanged.
- Both delivered theorems report exactly:

```text
[propext, Classical.choice, Quot.sound]
```