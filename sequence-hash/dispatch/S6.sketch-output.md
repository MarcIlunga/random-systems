Wrote [A6-tightness.md](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/sketches/A6-tightness.md).

1. Gaži’s exact loss is the row hybrid’s \(q\varepsilon_{\rm na}\), multiplied by the depth hybrid \(d=\ell+1\).
2. Backendal replaces the row hybrid with strong multi-user PRF security, yielding \((m+2)\varepsilon_{\rm mu}+\binom q2/|C|\); depth remains.
3. R4 is the tight ideal-compression analysis; R6 adds a standard-model route without query/user degradation rather than improving \(B_{\rm SEQ}\).
4. FIELD kills ambiguity events; role separation kills related-key and cross-role terms; same-role cascade collisions remain.
5. Backendal directly covers only the NMAC-normalized core; canonical SequenceFunction still needs a safe schedule normalization plus `DeriveCost_SEQ`.
6. The proposed G6 shape reuses the existing NMAC row laws, depth hybrid, PDS telescoping/DPI, birthday bounds, and domain-separation results; no theorem was generalized and no Lean/build command was run.