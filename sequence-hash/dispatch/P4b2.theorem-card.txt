TASK P4b2.theorem — R4 STRUCTURAL THEOREM, REDESIGNED (your dispatch-2 escalation was
correct; here is the resolved statement). The facade (dispatch 1) EXISTS — READ both first:
`RandomSystems/HTechnique/IdealCompression.lean` (generic: `TaggedBudgetRespects`, worlds,
representatives, reveal carrier, endpoint `adv_le_of_extFixedQueryRep_eq_on_good_filtered`)
and `SequenceHash/RandomSystems/IdealCompression.lean` (scheme: `sequenceFunctionIC{Real,Ideal}`,
`SequenceFunctionTraceBound`, `SequenceFunctionTaggedBudgetRespects`, reveal maps, KStepTotal).
ADDITIVE: new file `SequenceHash/RandomSystems/SequenceMACGeneric.lean` only; do NOT modify
R1/R2/R3/R5/DomainSeparation or the facade.

STATEMENT RESOLUTION (frozen — this fixes the escalation):
- `Bad_SEQ` is the COMPRESSION-CALL FRESHNESS-FAILURE event: some construction MD compression
  call `(chainingState, block)` coincides with a prior compression call — adversary `Prim`
  query OR another construction call, ANY role (same-role AND cross-role internal collisions).
  Byte-string DomainSeparation does NOT cover this (it separates full inputs, not internal
  `(chainingState, block)` pairs); the cross-role internal collision is a REAL cascade-inherent
  event and belongs INSIDE `Bad_SEQ` (bounded by `B_cascade`), NOT discharged by DomainSeparation.
- OFF `Bad_SEQ`, every construction compression call is fresh ⇒ on the ideal side each returns
  an independent uniform value ⇒ real extended mass = ideal independent-function mass. THIS is
  the equality-on-good (the standard H-coefficient "all fresh ⇒ real = ideal").
- `KeyPointMassBound` must express CONDITIONAL per-user min-entropy (or an independent product
  key law) — marginal point-mass alone does NOT imply the `KeyRepeat` term.

DELIVER (Codex's own finer split — do it, or STOP+split further via the valve):
1. Canonical MD compression traces (the `(chainingState, block)` call sequence of
   `sequenceFunctionIC{Real,Ideal}`), real/hybrid ideal reveal maps, trace reconstruction,
   length bounds — over canonical `sequenceFunction`/`mdHash` (reuse; do not re-spell).
2. `Bad_SEQ` as above; `KeyPointMassBound` (conditional min-entropy); `SequenceFunctionCrossRoleSeparated`
   discharged via DomainSeparation for the INPUT-level part it does cover.
3. The uniform-function partial-assignment fiber equality ⇒ equality of extended fixed-query
   masses OFF `Bad_SEQ`.
4. `B_SEQ` per A4 §5 (`B_cascade + B_key + deriveCostGeneric`; NOT `B_N`).
5. `h_badmass` NAMED (= R4.deep, not proven): `∀ E, SequenceFunctionTaggedBudgetRespects … E →
   Pr[Bad_SEQ ∣ sequenceFunctionICIdeal-extension … E] ≤ (B_SEQ … : ℝ)`.
6. FROZEN theorem `sequenceMAC_generic_prf_tight` (A4 §8 five features; conclusion
   `filteredAdaptiveTranscriptAdvantage (SequenceFunctionTaggedBudgetRespects …)
      (sequenceFunctionICReal …) (sequenceFunctionICIdeal …) ≤ (B_SEQ … : ℝ)`), closed via the
   eq-on-good endpoint (ε_ratio=0 from step 3) + `h_badmass`.

ACCEPTANCE: whole-repo `lake build` green; axiom-clean {propext, Classical.choice, Quot.sound};
sorry-free/axiom-free (only `h_badmass` is a named hypothesis, never a sorry); additive; NO
`private`; `B_SEQ` = §5; facade + DomainSeparation + R5 reused by name; closed via the endpoint.
Report: defs, `Bad_SEQ` + `KeyPointMassBound` exact statements, the frozen theorem, what
`h_badmass` states, reuse map, axiom line.

*** SPLIT VALVE: if steps 1+3 (traces/reveal/fiber equality) are too large to finish sorry-free
alongside the theorem, STOP and deliver ONLY steps 1–3 as a sorry-free foundation module, and
report the exact remaining theorem-dispatch plan. Never leave a `sorry`. ***
