Implemented in [SequenceMACIndiff.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:300).

Final R3 statement:

```lean
theorem sequenceMAC_prf_bound_indiff {L : U128} (q : ℕ) (b : BlockSize)
    (S : ByteString) (DK : RandomSystems.Dist SequenceMACKey)
    (D_H : RandomSystems.Dist (FixedHash L)) (ε_ind : ℕ → NNReal)
    (hDK : DK.isProbDist)
    (h_indiff : Δ(⌈q⌉ SM_H b S DK D_H, ⌈q⌉ SM_RO_separated b S DK)
      ≤ (ε_ind (4 * q) : ℝ)) :
    Δ(⌈q⌉ SM_H b S DK D_H,
        ⌈q⌉ PFunPDS.URF (X := InputSequence) (Y := HashOutput L))
      ≤ (ε_ind (4 * q) : ℝ) + (pairCollisionUnionBound (HashOutput L) q : ℝ)
```

The encoding leg directly extracts the existing `hsep` proof using:

- `maxAdvantage_filterQueries_seededHashThenURF_le`
- `sequenceMACSeparatedSeedDist_isProbDist`
- `sequenceMACSeparatedSeed_collision_mass_le`

R3 is one triangle through `SM_RO_separated`, followed by `add_le_add`.

Acceptance results:

- `lake build SequenceHash`: green, 8317 jobs.
- Forbidden-name grep: empty.
- File is sorry-free and contains no `private`.
- Fresh axiom checks for both new theorems:

```text
[propext, Classical.choice, Quot.sound]
```