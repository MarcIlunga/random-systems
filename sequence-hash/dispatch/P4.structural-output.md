Stopped under the mandatory split clause; no task files were created or edited.

Concrete blockers:

- A new `RandomSystems/HTechnique/*.lean` facade must be imported by `All.lean` or `LegacyChecks.lean` to pass `htechniqueSurfaceAudit`. That conflicts with “new files only / existing mtimes unchanged.” The manager must permit one import-line edit or relax the location requirement.
- The schematic worlds omit required ideal-compression parameters: `Block`, `MDCodec`, IV, digest length/state, and the relation `c = 8·L`.
- The H-technique query count must be fixed. Separate budgets `p` and `q` naturally require transcript length `p + q`; the displayed theorem leaves this implicit.
- Existing `adv_le_of_extFixedQueryRep_eq_on_good` is unfiltered. The reusable filtered representative endpoint is ratio-form only: `adv_le_of_extFixedQueryRep_ratio_of_good_filtered`. A filtered equality corollary should be added generically.
- Cross-role separation is conditional. Existing facts are `sequenceMACInnerInput_ne_outerInput`, `DeriveSafeS_SEQ`, `sequenceMAC_keyDerive_separated_of_not_prefixHit`, and `sequenceMAC_customizationDerive_separated_of_safe`. Therefore `SequenceFunctionCrossRoleSeparated` must mention `DK`/support and the good event must contain `DerivePrefixHit_SEQ`.
- Good-transcript equality is not currently a facade-level consequence. It needs an explicit compression-call trace, real/ideal reveal laws, `Bad_SEQ`, and a uniform-function fiber-count proof. Hiding that proof inside `hroles`, or defining badness as unequal masses, would violate the task.

Concrete split plan:

1. Facade infrastructure dispatch

   - Add reusable tagged `Prim`/`Eval` query accounting and `TaggedBudgetRespects`, fixing total transcript length to `p + q`.
   - Add the generic filtered equality-on-good representative endpoint.
   - Define typed ideal-compression coins, shared-`Prim` real/ideal worlds, canonical `sequenceFunction` evaluation, totality lemmas, and extension/reveal carriers.
   - Freeze the missing codec/IV/digest parameters and the exact type of `SequenceFunctionTraceBound`.
   - Permit the necessary `All.lean` import edit, or explicitly exempt the new H-technique module from the coverage audit.

2. Structural theorem dispatch

   - Define concrete `KeyPointMassBound`, `SequenceFunctionCrossRoleSeparated`, compression traces, and the five-event `Bad_SEQ`.
   - Prove equality of extended fixed-query masses off `Bad_SEQ`, using the DomainSeparation and R5 bridges above.
   - Define `B_cascade`, `B_key`, `deriveCostGeneric`, and `B_SEQ : NNReal` exactly as §5.
   - State `h_badmass` precisely as:
     ```lean
     ∀ E, EnvRespects (TaggedBudgetRespects …) E →
       Pr[Bad_SEQ ∣ sequenceFunctionICIdealExtension … E.1] ≤ B_SEQ …
     ```
   - Close `sequenceMAC_generic_prf_tight` with the filtered equality-on-good endpoint and run both SequenceHash and whole-repository gates plus `#print axioms`.

The pre-existing working-tree changes were left untouched.