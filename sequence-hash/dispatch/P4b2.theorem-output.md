Stopped under the split valve. No files were created or modified.

The frozen theorem is currently false at the facade’s level of abstraction:

- [`MDCodec`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/MDHash.lean:24) does not require `blockify message` to be nonempty.
- Consequently, [`mdHash`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/MDHash.lean:73) may evaluate an outer input as `mdIterate h iv [] = iv`, making the real result constant.
- [`SequenceFunctionTraceBound`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/IdealCompression.lean:64) only supplies upper bounds. It cannot prove that the outer trace contains a final compression call.
- Such an empty outer trace does not satisfy the frozen definition of `Bad_SEQ`: no construction call repeats a previous call. Nevertheless, real extended mass is concentrated at `iv`, while the ideal `Eval` reply remains uniform. Thus equality-on-good fails.
- Domain-separation theorems distinguish byte-string inputs; they cannot create a missing compression call.

A new assumption is therefore necessary. Recommended:

```lean
def SequenceFunctionOutputCompressionBacked
    (model : SequenceFunctionCompressionModel Block L)
    (b : BlockSize) (S : ByteString) : Prop :=
  ∀ K M derivedK derivedS inner,
    (model.codec.blockify
      (sequenceFunctionOuterInput b fSeqMac K.1 S M
        derivedK derivedS inner)).Nonempty
```

It can either become an explicit hypothesis of `sequenceMAC_generic_prf_tight`, or a field of `SequenceFunctionCompressionModel`. Folding it into `SequenceFunctionCrossRoleSeparated` would be misleading; extending `Bad_SEQ` would violate the frozen event and require changing `B_SEQ`.

After that resolution, the exact remaining split is:

1. Canonical `mdCompressionTrace`, role-tagged `sequenceFunctionCompressionTrace`, reconstruction, final-output, and length/padding theorems.
2. `Bad_SEQ`, with all `Prim` inputs ordered before construction calls; conditional `KeyPointMassBound`; input-level cross-role discharge using `sequenceMACInnerInput_ne_outerInput`, `sequenceMAC_keyDerive_separated_of_not_prefixHit`, and `sequenceMAC_customizationDerive_separated_of_safe`.
3. Uniform partial-assignment fiber equality and concrete extended fixed-query mass equality off `Bad_SEQ`.
4. `B_cascade`, `B_key`, `deriveCostGeneric`, `B_SEQ`, named `h_badmass`, and closure through `adv_le_of_extFixedQueryRep_eq_on_good_filtered`.

No build or axiom claim is reported because the requested artifact could not soundly be created under the frozen assumptions.