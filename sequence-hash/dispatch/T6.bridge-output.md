Stopped under the mandated escalation rule: the frozen bridge is not provable as stated.

Why:

- [`ProtocolIndifferentiableSequenceMACBridge`](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:399) quantifies over an arbitrary `MetricResourceAlgebra A`. Nothing relates `R_H`, `R_RO`, or their attached quotient resources to the concrete PDS systems `SM_H` and `SM_RO`.
- `ProtocolIndifferentiable` only bounds the distance between `attachQ con π_H R_H` and a simulator-attached `R_RO`; it does not directly compare the two frozen PDS targets.
- The concrete carrier must be fixed to `RandomSystemsCC.probMetricAlgebra n`, with explicit realization/projection equalities identifying those attached worlds with the filtered `SM_H` and `SM_RO`.
- The current concrete carrier’s converters are only memoryless endomaps `(X → X) × (Y → Y)` ([DetAlgebra.lean](/Users/marcilunga/Documents/tob/research/random-systems/RandomSystemsCC/DetAlgebra.lean:135)); they cannot represent the type-changing, four-call `sequenceMACConverter`. A memoryful/type-changing carrier attachment or an explicit quotient-world realization theorem is required.
- [`edistD_le_maxAdvantage`](/Users/marcilunga/Documents/tob/research/random-systems/RandomSystemsCC/ProbMetric.lean:668) has direction `edistD ≤ max Δ`. The required `Δ ≤ ε` direction needs [`ofReal_maxAdvantage_le_edistD`](/Users/marcilunga/Documents/tob/research/random-systems/RandomSystemsCC/VerdictBehavior.lean:524) or [`edistD_eq_maxAdvantage`](/Users/marcilunga/Documents/tob/research/random-systems/RandomSystemsCC/VerdictBehavior.lean:562).
- `Sponge.lean` documents this intended linkage but does not contain a completed `ProtocolIndifferentiable`-to-PDS bridge to mirror.

Required statement change: specialize `A := RandomSystemsCC.probMetricAlgebra n` and replace unconstrained `R_H/R_RO` with concrete `ResPT` representatives—or add hypotheses equating representatives of both attached quotient worlds, including the simulator side, to the tagged filtered `SM_H` and `SM_RO` laws. The carrier must also support the actual SequenceMAC protocol converter.

No files were changed. `lake build SequenceHash` remains green: 8,332 jobs. The unconditional theorem was not added because doing so would require fabricating the forbidden bridge.