Closed `sequenceMAC_prf_bound` in [SequenceMACPRF.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:1545).

Proof:

- Unfolds `epsKS`.
- Applies `maxAdvantage_triangle`.
- Uses `gcongr` and the existing `nmac_prf_bound`, threading `hna`.
- Normalizes addition with `ring`.

Verification:

- `lake build SequenceHash.RandomSystems.SequenceMACPRF` — green.
- Axioms: `[propext, Classical.choice, Quot.sound]`; no `sorryAx`.
- Entire R2 file is `sorry`/`admit`-free.
- No `private` declarations.
- Edited proof remains pure CR18; no advantage wrappers or game/oracle objects.