Implemented the mechanical triangle proof in [SequenceMACIndiff.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACIndiff.lean:245).

Reused:

- `maxAdvantage_triangle`
- `h_bridge h_ind hDK hH q`
- `h_enc hDK q`
- `add_le_add`
- `simp [ε_enc]` to normalize `ε_sched + pairCollisionUnionBound`

Verification:

- `lake build SequenceHash` — green, 8,332 jobs.
- Axiom check — only `propext`, `Classical.choice`, and `Quot.sound`.
- `SequenceMACIndiff.lean` now contains zero `sorry`, `admit`, `axiom`, or `private`.
- The frozen theorem statement and both obligation definitions were unchanged.
- No R1/R2 files were edited or hypotheses re-derived.

The guardrail is therefore sorry-free given `h_bridge` and `h_enc`.