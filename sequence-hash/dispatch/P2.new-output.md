Implemented [CascadeNAPF.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/CascadeNAPF.lean:1).

Public statement:

```lean
theorem cascade_na_pf_adv_le ... :
  cascadeNAPFAdv q ℓ f ≤
    (((ℓ * q : ℕ) : ℝ) * advNPRF (q := q) (compSystem f))
```

It is generic over `B`, `C`, and `f : C → B → C`, with:

- `cascadeSystem : ProbPDS (BlockString B ℓ) C`
- uniform-key `compSystem`
- independently keyed `multiCompSystem`
- prefix-freeness based on `CBCMAC.PrefixFree`
- explicit Appendix A depth and row hybrid families
- repeated queries deduplicated through the query range

Reused `mdIterate`, `Dist.prodProbDist`, `Dist.fTransform`, `advNPRF`, the canonical prefix-free predicate, and imported the framework telescoping infrastructure. The frozen `SequenceMACPRF.lean` was untouched.

Verification:

```text
lake build SequenceHash.RandomSystems.CascadeNAPF
Build completed successfully
```

Two residual `sorry`s remain, precisely at the deepest permitted steps:

- `gazi_lemma5_depth_hybrid`: adjacent depth-law identities, including prefix-row sharing, prefix-free endpoint identification, `mdIterate_append`, and telescoping.
- `gazi_lemma6_row_hybrid`: adjacent row replacement reduction, uniform-curried-row/URF endpoint equivalence, and fixed-query telescoping into `advNPRF`.

The final Proposition 1 theorem is proved from these two correctly stated intermediate inequalities.