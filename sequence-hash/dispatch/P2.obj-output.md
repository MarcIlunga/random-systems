Implemented P2.obj.

- [MDHash.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/MDHash.lean:14)
  - Generalized `Compression`/`mdIterate` to the faithful transition `State → Block → State`.
  - Added `MDCodec` with hypotheses for positive block size, native-padding realization, full-block output, and prefix-free strengthening.
  - Added `mdHash` implementing `MD[f]`.
  - Reused `List.foldl_append` directly for `mdIterate_append`; no cascade dependency or reproof.

- [SequenceMACPRF.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:43)
  - Replaced `compSystem` with the uniform-key `PFunPDS.Prob.functionEvaluator`.
  - Left the four construction-specific stubs and goal proof unchanged.
  - `sequenceMAC_prf_bound` statement remains untouched.

Both requested `lake build` gates pass. No new `axiom`; remaining `sorry`s are exactly the four stubs plus the guardrail theorem.