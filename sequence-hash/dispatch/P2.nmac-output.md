Implemented the Gaži proof scaffold in [SequenceMACPRF.lean](/Users/marcilunga/Documents/tob/research/random-systems/sequence-hash/SequenceHash/RandomSystems/SequenceMACPRF.lean:161).

- `nmac_prf_bound` now closes via a two-hop `Δ` trace using `maxAdvantage_le_adjacent_sum`.
- Proposition 1 composes the length-position and row-index bounds, producing `d·q·εna`.
- Reused `PFunPDS`, fixed-query transcript laws, `CBCMAC.PrefixFree`, `Dist.prod`/`fTransform`, and framework telescoping.
- Frozen theorem statements are unchanged.
- No `advPRF`, `advNPRF`, `Adv`, or game objects are used.
- `lake build SequenceHash.RandomSystems.SequenceMACPRF` passes.

Four deepest residual `sorry`s remain:

1. `gazi_lemma6_row_hybrid` — Appendix A Lemma 6, Eq. (10).
2. `gazi_lemma5_depth_hybrid` — Appendix A Lemma 5, Eq. (9), including `mdIterate_append`.
3. `gazi_outer_prf_hop` — §3.1 outer `f_K₂ → r` substitution and randomized-converter DPI.
4. `gazi_outer_random_collision_bound` — §3.1 condition `C`, Eq. (2), prefix-free extension, collision tester, and birthday bound.

The existing `sequenceMAC_prf_bound` final-triangle `sorry` remains untouched as requested.